---
description: Include all external libraries via CDN. Do not install packages via npm or similar package managers.
globs: 
alwaysApply: false
---
## ✅ CDN-Only Libraries

All external libraries (e.g., Shoelace) must be included via CDN.

Do not use:
- npm
- Yarn
- pnpm
- `node_modules` directory
